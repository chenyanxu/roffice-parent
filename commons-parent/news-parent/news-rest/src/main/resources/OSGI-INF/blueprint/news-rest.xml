<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xsi:schemaLocation="http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
       http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">

    <reference id="newsService" interface="cn.com.rexen.roffice.news.api.biz.INewsBeanService"/>

    <camelContext id="newsCamel" xmlns="http://camel.apache.org/schema/blueprint">

        <restConfiguration component="servlet" bindingMode="json" contextPath="/kalix/camel/rest" host="localhost"
                           port="8181">
            <dataFormatProperty key="prettyPrint" value="true"/>
        </restConfiguration>

        <!-- defines the rest services using the context-path /news -->
        <rest path="/newss" consumes="application/json" produces="application/json">
            <description>news rest service</description>

            <!-- this is a rest GET to find all news -->
            <get id="route-getAllNews" outType="cn.com.rexen.core.api.persistence.JsonData">
                <description>Find all Newss</description>
                <to uri="bean:newsService?method=getAllEntityByQuery(${header.page},${header.limit},${header.jsonStr})"/>
            </get>

            <!-- this is a rest POST to create a news -->
            <post id="route-addNews" type="cn.com.rexen.roffice.news.entities.NewsBean"
                  outType="cn.com.rexen.core.api.biz.JsonStatus">
                <description>Create a News</description>
                <to uri="bean:newsService?method=saveEntity"/>
            </post>

            <!-- this is a rest GET to view a news by the given id -->
            <get uri="/{id}" id="route-getNews" outType="cn.com.rexen.roffice.news.entities.NewsBean">
                <description>Find News by id</description>
                <to uri="bean:newsService?method=getEntity(${header.id})"/>
            </get>

            <!-- this is a rest PUT to update a news -->
            <put uri="/{id}" id="route-updateNews" type="cn.com.rexen.roffice.news.entities.NewsBean"
                 outType="cn.com.rexen.core.api.biz.JsonStatus">
                <description>Updates a News</description>
                <to uri="bean:newsService?method=updateEntity"/>
            </put>

            <!-- this is a rest DELETE to delete a news -->
            <delete uri="/{id}" id="route-deleteNews" type="cn.com.rexen.roffice.news.entities.NewsBean"
                    outType="cn.com.rexen.core.api.biz.JsonStatus">
                <description>Delete a News</description>
                <to uri="bean:newsService?method=removeEntity"/>
            </delete>

        </rest>
    </camelContext>
</blueprint>